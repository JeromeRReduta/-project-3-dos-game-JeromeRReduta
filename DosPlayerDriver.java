import java.util.*;
public class DosPlayerDriver {
    /*public static void main(String[] args) {

        Random rng = new Random();

        DosDeck deck = new DosDeck();
        DosPlayer a = new DosPlayer("Bob");
        DosDeck centerRow = new DosDeck();


        deck.fillDeck();
        deck.shuffle(rng);

int i = 0;
        while (i < 7) {
            deck.moveCard(deck.getCard(0), a.getHand());
            i++;
        }

        centerRow.addCard(deck.getCard(0));
        centerRow.addCard(deck.getCard(1));

        // System.out.println(deck.getSize());
        System.out.println(a);
        // System.out.println(a.getHand());

        a.moveCard(a.getHand().getCard(0), deck);

        // System.out.println(a.getHand());

        System.out.println(a.displayInfo());

        System.out.println(centerRow);

        a.getMatches(centerRow);


    } */
}
